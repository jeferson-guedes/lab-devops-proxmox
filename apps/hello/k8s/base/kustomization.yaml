apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - network-policy.yaml

labels:
  - pairs:
      app: my-gitops-api
    includeSelectors: true
